# Admin tool env. Copy to `.env.local` for dev.

# API origin (same Worker as the customer app).
NEXT_PUBLIC_API_URL=http://localhost:8787

# Customer app URL — used to open impersonation sessions in a new tab.
NEXT_PUBLIC_CUSTOMER_APP_URL=http://localhost:3000
