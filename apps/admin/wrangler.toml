# Cloudflare Pages config for the admin tool.
name = "admin"
pages_build_output_dir = ".next"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[env.preview]
# Per-PR previews — Cloudflare Pages auto-creates preview URLs.

[env.staging]
# REPLACE_WITH_STAGING_ADMIN_PAGES_PROJECT_NAME

[env.production]
# REPLACE_WITH_PROD_ADMIN_PAGES_PROJECT_NAME
