/**
 * Query helpers — added in later phases.
 * Backend Agent reads schemas; helpers will be added here as endpoints are built.
 *
 * Planned helpers (per database.md):
 *   - getCallsForOrganization(orgId, filters)
 *   - createCall(callData)
 *   - getActiveSubscription(orgId)
 *   - markPromoCodeRedeemed(codeId, orgId)
 */
export {};
